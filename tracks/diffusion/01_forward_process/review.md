# Review questions

- What does `beta_t` control?
- Why does `alpha_bar_t` decrease over time?
- Why is the closed-form `q(x_t | x_0)` useful for training?
- What is the shape of a gathered coefficient for image tensors?
- Why does `q_sample` return the sampled noise as well as `x_t`?
