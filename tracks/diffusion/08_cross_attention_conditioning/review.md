# Review questions

- Where do queries, keys, and values come from in cross-attention?
- How does cross-attention differ from causal self-attention?
- Why are position embeddings needed for prompt tokens?
- Why must padding scores be masked before softmax?
- Why is prompt context reused at every reverse diffusion step?
- Which models receive gradients from the conditioned diffusion loss?
- How would classifier-free guidance extend this prompt-conditioned model?
