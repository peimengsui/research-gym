# Review questions

- What are the endpoints of this lesson's probability path?
- Why is the straight-path target velocity `data - noise`?
- What does the velocity network receive as input?
- How does flow-matching training differ from epsilon prediction?
- What accuracy/cost trade-off separates Euler and midpoint integration?
- Why does generation integrate from `t=0` to `t=1` in this convention?
- Which extensions would be needed for prompt-conditioned latent flow matching?
