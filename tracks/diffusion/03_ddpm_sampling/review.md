# Review questions

- Why does sampling start from Gaussian noise?
- Why should the final step at `t = 0` not add extra noise?
- How is `x0_pred` different from the reverse mean?
- Why is an oracle denoiser useful for testing but not a real generator?
- Which schedule values are reused from the forward process?
