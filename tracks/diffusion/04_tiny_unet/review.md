# Review questions

- Why is a U-Net a natural fit for image-shaped diffusion?
- Why do skip connections help preserve spatial detail?
- Where does timestep information enter the network?
- Why does the model output have the same shape as the noise target?
- What would need to change for RGB images instead of one-channel images?
