# Review questions

- Why must some conditions be dropped during training?
- What does the learned null class represent?
- Why does CFG require two model calls per sampling step?
- What do guidance scales `0`, `1`, and values above `1` mean?
- Why is the method called classifier-free even when it uses class labels?
- What quality/diversity trade-off can appear at high guidance scales?
- Which parts of DDIM remain unchanged after guidance is added?
