# Review questions

- Why can latent diffusion be cheaper than pixel-space diffusion?
- Why are autoencoder reconstruction loss and diffusion loss separate?
- Why should the autoencoder stay frozen during denoiser training?
- What problem does latent scaling solve?
- Where must inverse scaling happen during generation?
- What image information can a perfect latent denoiser still fail to recover?
- Which parts of DDIM change when its tensors represent latents instead of pixels?
