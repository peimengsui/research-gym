# Review questions

- Why can the training loop know the exact noise target?
- Why does the denoiser need timestep conditioning?
- What shape should the predicted noise have?
- How is this objective different from directly predicting `x_0`?
- How will predicted noise be useful in a reverse sampling step?
