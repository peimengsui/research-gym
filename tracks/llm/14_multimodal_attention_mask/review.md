# Review questions

- Why is the separator counted inside the prefix length?
- Why is the visual prefix bidirectional instead of causal?
- How does a text token see the image without attending to future text?
- What goes wrong if padding keys are left unmasked?
- Why zero padding query rows after softmax?
- How does this 2D matrix relate to the 1D validity mask from lesson 13?
- What would change if image tokens were inserted between text tokens instead of as a prefix?
- Which later multimodal component will consume this attention matrix?
