# Review questions

- Why does lower temperature sharpen a token distribution?
- How do top-k and top-p differ when model confidence changes?
- Why are removed logits set to negative infinity rather than zero?
- Why should softmax happen after filtering?
- What is the difference between greedy decoding and low-temperature sampling?
- Why must generation crop model context but preserve full output history?
- Which sampling choices would you expose in a user-facing generation API?
