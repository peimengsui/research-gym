# Reflection questions

- Why does BPE begin with small tokens rather than whole words?
- Why must training and encoding apply merges in the same order?
- Why are non-overlapping replacements important?
- What does deterministic tie-breaking buy us?
- How does `<unk>` differ from a byte-level fallback?
- What tradeoff changes as the number of merges increases?
