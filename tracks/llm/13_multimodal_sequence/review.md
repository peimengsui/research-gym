# Review questions

- Why must visual and text tokens have the same embedding width?
- What purpose does the learned separator serve?
- How do token-type embeddings differ from sequence-position embeddings?
- Why can visual tokens receive both spatial and full-sequence positions?
- Why are padded text embeddings retained in the rectangular sequence?
- What does the one-dimensional attention mask describe at this stage?
- Why is it useful to return mask and type metadata beside the embeddings?
- Which additional masking rule is still needed for autoregressive text?
