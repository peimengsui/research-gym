# Review questions

- Why can a target verification score multiple draft positions together?
- When is a drafted token accepted with probability one?
- Why does rejection discard the remaining draft suffix?
- Why is the correction based on positive `p - q` rather than `p`?
- Where does the bonus token come from?
- Why can exact speculative and baseline samples differ for one seed?
- How can empirical frequency tests check distribution preservation?
- What determines whether speculative decoding produces a real speedup?
- Why is acceptance rate insufficient without considering draft cost?
- How would caches be rolled back after a partially accepted draft block?
